# Uveddi CLI Help Output
**Generated:** 2025-10-09  
**Version:** 1.0.0  
**Features:** cli-standard  

This document captures the complete CLI help output for the 1.0.0 product surface freeze.

## Main Help

```
A Rust-based code analysis and exploration tool
Usage: uveddi <COMMAND>
Commands:
  analyze  Perform comprehensive code analysis
  config   Manage configuration settings
  doctor   Run health diagnostics and fix common issues
  help     Show help information
  hooks    Git hooks management
  init     Initialize Uveddi configuration for a project
  ci       CI/CD integration command
  help     Print this message or the help of the given subcommand(s)
Options:
  -h, --help     Print help
  -V, --version  Print version
```

## Subcommand Help

### `uveddi analyze`

```
Perform comprehensive code analysis
Usage: uveddi analyze [OPTIONS] <PATH>
Arguments:
  <PATH>
          Path to the project or directory to analyze
          
          Can be a file or directory. When analyzing a directory, all supported source files will be recursively processed.
Options:
      --output-format <OUTPUT_FORMAT>
          Output format for the analysis report
          
          Supported formats: - `text`: Plain text format for terminal output - `json`: Structured JSON format for programmatic consumption - `markdown`: Markdown format for documentation - `html`: Interactive HTML format with embedded diagrams and dark/light themes
          
          [default: markdown]
      --output <OUTPUT>
          Optional output file path
          
          If not specified, the report will be written to stdout. The file extension should match the chosen output format.
      --enable-ai
          Enable AI-powered analysis and explanations
          
          When enabled, the analysis will include AI-generated explanations for detected issues and architectural recommendations. Requires either an API key or a local Ollama instance.
      --ollama-api-url <OLLAMA_API_URL>
          Ollama API URL for local AI analysis
          
          Used when `--enable-ai` is specified and you want to use a local Ollama instance instead of external AI services.
          
          Can also be set via the `OLLAMA_API_URL` environment variable.
          
          [env: OLLAMA_API_URL=]
      --ollama-model <OLLAMA_MODEL>
          Ollama model name for local AI analysis
          
          Specifies which Ollama model to use for analysis. Common options include "deepseek-coder:6.7b-instruct-q4_0", "codellama:7b-instruct", etc.
          
          Can also be set via the `OLLAMA_MODEL` environment variable.
          
          [env: OLLAMA_MODEL=]
      --dead-code-confidence <THRESHOLD>
          Confidence threshold for dead code detection (0.0 to 1.0)
          
          Only report dead code issues with confidence above this threshold. Higher values reduce false positives but may miss some issues.
      --dead-code-library-mode
          Enable library mode for dead code detection
          
          In library mode, exported symbols are treated more conservatively to avoid false positives for public APIs.
      --dead-code-ignore-patterns <DEAD_CODE_IGNORE_PATTERNS>
          Patterns to ignore during dead code detection
          
          Comma-separated list of patterns to exclude from analysis. Example: "test,spec,mock,generated"
      --dead-code-keep-alive <DEAD_CODE_KEEP_ALIVE>
          Symbols to always keep alive during dead code detection
          
          Comma-separated list of symbol patterns that should never be reported as dead code. Example: "main,init,setup,teardown"
      --large-classes-max-loc <LINES>
          Maximum logical lines of code threshold for large classes
          
          Classes exceeding this threshold will be flagged as potentially too large. Default varies by language (Rust: 400, Python: 1000, JavaScript: 800)
      --large-classes-max-methods <COUNT>
          Maximum number of methods threshold for large classes
          
          Classes with more methods than this threshold will be flagged. Default varies by language (Rust: 20, Python: 20, JavaScript: 25)
      --large-classes-max-fields <COUNT>
          Maximum number of fields threshold for large classes
          
          Classes with more fields than this threshold will be flagged. Default varies by language (Rust: 15, Python: 7, JavaScript: 12)
      --large-classes-max-complexity <COMPLEXITY>
          Maximum cyclomatic complexity threshold for large classes
          
          Classes with higher complexity will be flagged as potentially too complex. Default varies by language (Rust: 50, Python: 60, JavaScript: 55)
      --large-classes-max-lcom <SCORE>
          Maximum LCOM (Lack of Cohesion in Methods) score threshold
          
          Higher values indicate lower cohesion. Range: 0.0 to 1.0 Default: 0.8 for all languages
      --large-classes-ignore-patterns <LARGE_CLASSES_IGNORE_PATTERNS>
          Patterns to ignore during large classes detection
          
          Comma-separated list of patterns to exclude from analysis. Example: "test,spec,mock,generated,fixture"
      --large-classes-min-severity <SCORE>
          Minimum severity score for large classes reporting (0-100)
          
          Only report issues with severity above this threshold. 0-25: Info, 26-50: Low, 51-75: Medium, 76-90: High, 91-100: Critical
          
          [default: 25]
      --disable-memory-optimization
          Disable memory optimization features
          
          By default, Uveddi uses memory optimization (object pooling, arena allocation, and zero-copy AST caching) for better performance. Use this flag to disable optimizations.
      --memory-limit-gb <GB>
          Memory limit in gigabytes for analysis
          
          Sets a soft limit on memory usage. The analysis will attempt to stay within this limit by using more aggressive memory management.
      --memory-profile <PROFILE>
          Memory profile for optimization settings
          
          Selects pre-configured memory optimization settings: - `small`: Optimized for small projects (< 1000 files) - `default`: Balanced settings for most projects (default) - `large`: Optimized for large codebases (> 10000 files)
          
          [default: default]
      --enable-image-rendering
          Enable image rendering (requires rendering service)
          
          When enabled, diagrams will be rendered as images using the rendering service. Falls back to Mermaid-only mode if service is unavailable (unless --no-fallback is used).
      --mermaid-only
          Force Mermaid-only mode (no image rendering, zero hosting costs)
          
          Generate only Mermaid code with helpful rendering instructions. This is the default mode to eliminate hosting costs.
      --rendering-service-url <RENDERING_SERVICE_URL>
          Rendering service URL
          
          URL of the rendering service for image generation. Only used when --enable-image-rendering is specified.
          
          [default: http://localhost:3001]
      --no-fallback
          Disable fallback to Mermaid-only (fail if image rendering unavailable)
          
          When enabled, analysis will fail if image rendering is requested but unavailable. By default, the system gracefully falls back to Mermaid-only mode.
      --check-rendering-service
          Check rendering service availability without running analysis
          
          Performs a health check on the rendering service and exits. Useful for verifying service configuration before running analysis.
      --no-diagrams
          Disable diagram generation completely
          
          Skip all diagram generation to speed up analysis when only textual output is needed.
      --max-diagrams <MAX_DIAGRAMS>
          Maximum number of diagrams to generate per report
          
          Limits the total number of diagrams to prevent performance issues with large codebases. Set to 0 for no limit.
          
          [default: 20]
      --diagram-output-dir <DIAGRAM_OUTPUT_DIR>
          Output directory for diagram files
          
          When using image rendering modes, diagrams will be saved to this directory. If not specified, a subdirectory next to the report will be created.
      --timeout <TIMEOUT>
          Maximum analysis timeout in seconds
          
          Sets the maximum time to wait for analysis completion before aborting. Default is 300 seconds (5 minutes). Set to 0 to disable timeout.
          
          [default: 300]
      --verbose
          Show detailed error information and stack traces
          
          When enabled, errors will include additional debug information, stack traces, and context to help diagnose issues.
      --progress-format <PROGRESS_FORMAT>
          Progress reporting format
          
          Controls how progress is displayed during analysis: - `terminal`: Rich terminal output with progress bars and estimates (default) - `json`: JSON progress events for programmatic consumption - `silent`: No progress output (quiet mode)
          
          [default: terminal]
      --progress-details
          Show detailed progress information
          
          When enabled with terminal progress format, displays current file being processed, throughput metrics, and detailed timing information.
      --open-dashboard
          Automatically open dashboard after analysis completes
          
          When enabled, the analysis dashboard will be launched in the default web browser after analysis is complete, showing the results in a visual interface.
      --security
          Enable security vulnerability analysis
          
          Performs comprehensive security analysis including OWASP Top 10 coverage, taint flow analysis, and vulnerability detection.
      --security-only
          Run only security analysis (skip other detectors)
          
          When enabled, only security-related anti-pattern detection will be performed. This provides faster analysis when only security concerns are relevant.
      --min-security-confidence <THRESHOLD>
          Minimum confidence threshold for security findings (0.0 to 1.0)
          
          Only report security issues with confidence above this threshold. Higher values reduce false positives but may miss some vulnerabilities.
          
          [default: 0.5]
      --export-sarif
          Export security findings in SARIF 2.1.0 format
          
          Generates SARIF-compliant output suitable for GitHub Security tab, CI/CD pipeline integration, and security toolchain interoperability.
      --sarif-output <SARIF_OUTPUT>
          SARIF output file path
          
          Specifies where to save the SARIF export file. If not provided, defaults to '<output-file-stem>.sarif' or 'security-findings.sarif'.
      --enable-taint-analysis
          Enable taint flow analysis for data flow vulnerabilities
          
          Performs source-to-sink taint analysis to identify potential injection vulnerabilities and unsafe data flows.
      --taint-analysis-depth <DEPTH>
          Maximum depth for taint flow analysis (1-20)
          
          Controls how deep the taint analysis traverses call chains. Higher values increase accuracy but also analysis time.
          
          [default: 10]
      --owasp-categories <OWASP_CATEGORIES>
          OWASP categories to focus on (comma-separated)
          
          Limit security analysis to specific OWASP Top 10 2021 categories. Examples: "A01,A03,A06" or "injection,broken_access_control"
  -h, --help
          Print help (see a summary with '-h')
```

### `uveddi config`

```
Manage configuration settings
Usage: uveddi config <COMMAND>
Commands:
  show      Show the current configuration (from file or environment)
  set       Set a configuration value in the config file
  validate  Validate the configuration file with smart suggestions
  help      Print this message or the help of the given subcommand(s)
Options:
  -h, --help  Print help
```

### `uveddi doctor`

```
Run health diagnostics and fix common issues
Usage: uveddi doctor [OPTIONS]
Options:
      --fix                            Automatically fix issues where possible
      --parsers                        Check only language parser availability and functionality
      --ai                             Check only AI service connectivity and model availability
      --system                         Check only system resources and file permissions
      --output-format <OUTPUT_FORMAT>  Output format: human, json, markdown [default: human]
      --extended                       Run extended functionality tests (slower but more comprehensive)
      --quiet                          Only report critical issues, suppress warnings
  -h, --help                           Print help
```

### `uveddi hooks`

```
Git hooks management
Usage: uveddi hooks <COMMAND>
Commands:
  install    Install Git hooks for automated analysis
  uninstall  Uninstall Uveddi Git hooks
  list       List installed Git hooks
  test       Test Git hooks without committing
  config     Show hook configuration
  help       Print this message or the help of the given subcommand(s)
Options:
  -h, --help  Print help
```

### `uveddi init`

```
Initialize Uveddi configuration for a project
Usage: uveddi init [OPTIONS] [PATH]
Arguments:
  [PATH]
          Path to initialize Uveddi configuration
          
          [default: .]
Options:
      --non-interactive
          Skip interactive prompts and use defaults
      --template <TEMPLATE>
          Project template to use
          Possible values:
          - rust:        Rust project (Cargo-based)
          - python:      Python project (pip/poetry-based)
          - java-script: JavaScript/Node.js project
          - type-script: TypeScript project
          - web:         Web project (mixed JS/TS/HTML/CSS)
          - library:     Library project (multi-language)
          - monorepo:    Monorepo with multiple languages
          - custom:      Custom configuration
      --force
          Force overwrite existing configuration
      --git-hooks
          Generate Git hooks for automated analysis
  -h, --help
          Print help (see a summary with '-h')
```

### `uveddi ci`

```
CI/CD integration command
Usage: uveddi ci <COMMAND>
Commands:
  check  Run analysis and fail if thresholds are exceeded (for CI quality gates)
  help   Print this message or the help of the given subcommand(s)
Options:
  -h, --help  Print help
```

---

**Generated with Uveddi version 1.0.0**
