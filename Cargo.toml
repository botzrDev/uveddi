# All dependencies are now pinned to specific versions for reproducible builds.

[package]
name = "uveddi"
version = "0.0.2"
edition = "2021"
resolver = "2"
description = "A Rust-based code analysis and exploration tool"
authors = ["Phillip Austin Green <your.email@example.com>"]
license = "CC-BY-NC-SA-4.0"
repository = "https://github.com/botzrDev/uveddi"
keywords = ["code", "analysis", "tools", "visualization", "uveddi"]
categories = ["development-tools"]
readme = "README.md"
autobins = false  # Disable auto-discovery of binaries

# Workspace dependency management for version consistency
[workspace.dependencies]
# Core serialization - single version across workspace
serde = { version = "1.0.219", features = ["derive"] }
serde_json = "1.0.143"
serde_yaml = "0.9.34"

# Async runtime - unified tokio ecosystem
tokio = { version = "1.47", features = ["full"] }
futures = "0.3.31"

# Error handling - standardized approach
anyhow = "1.0.86"
thiserror = "2.0.16"

# Logging - unified logging infrastructure
tracing = { version = "0.1.41", features = ["log"] }
tracing-subscriber = { version = "0.3.20", features = ["env-filter", "json"] }

# HTTP client - single version with needed features
reqwest = { version = "0.12.22", default-features = false, features = ["json", "rustls-tls"] }

# Cryptography - consistent versions
rustls = "0.23.31"

# Date/time handling
chrono = { version = "0.4.41", features = ["serde"] }

# System directories
dirs = "5.0"

# String similarity for command suggestions
levenshtein = "1.0"

# Feature flags for modular compilation - CLI-focused release
[features]
# ==================== CLI-FOCUSED PROFILES ====================
# Default profile for CLI users
default = ["cli-standard"]

# Absolute minimum for CLI functionality
cli-core = [
    "dep:rusqlite",
    "dep:bincode", 
    "dep:rayon",
    "dep:tokio-stream",
    "dep:async-stream",
    "dep:reqwest"
]

# Standard CLI with all language support (RECOMMENDED)
cli-standard = [
    "cli-core",
    "tree-sitter",
    "engine-integration",
    "ast-cache",
    "analysis-cache",
    "security",
]

# CLI with AI support (requires Ollama)
cli-ai = ["cli-standard", "ai", "local-ai"]

# CLI with plugin support
cli-plugins = ["cli-standard", "wasm-plugins"]

# Everything for CLI (AI + plugins)
cli-full = ["cli-ai", "cli-plugins"]

# ==================== LANGUAGE PARSING ====================
# Individual language support (granular control)
rust-lang = ["dep:tree-sitter-rust"]
python-lang = ["dep:tree-sitter-python"]
javascript-lang = ["dep:tree-sitter-javascript"]
typescript-lang = ["dep:tree-sitter-typescript"]

# ==================== LANGUAGE PACKS ====================
# Backend languages (Rust, Python)
languages-core = ["rust-lang", "python-lang"]

# Frontend languages (JavaScript, TypeScript)
languages-web = ["javascript-lang", "typescript-lang"]

# All supported languages
languages-all = ["languages-core", "languages-web"]

# Full tree-sitter support (all languages)
tree-sitter = [
    "dep:tree-sitter",
    "languages-all"
]

# ==================== OPTIONAL FEATURES ====================
# Caching systems
ast-cache = ["dep:notify"]
analysis-cache = ["dep:notify"]

# AI and analysis features
ai = []
local-ai = ["ai", "dep:reqwest"]

# Plugin and extension systems
wasm-plugins = ["dep:wasmtime", "dep:wasmtime-wasi", "dep:cap-std", "dep:wit-bindgen"]

# Engine refactor integration
engine-integration = ["tree-sitter"]

# Security analysis and reporting features
security = []

# ==================== LEGACY PROFILES (DEPRECATED - CLI ONLY) ====================
# DEPRECATED: Use 'cli-core' instead
minimal = ["cli-core"]

# DEPRECATED: Use 'cli-standard' instead
standard = ["cli-standard"]

# DEPRECATED: Use 'cli-full' instead
full = ["cli-full"]

# DEPRECATED: Use 'cli-standard' instead
dev-core = ["cli-standard"]

# DEPRECATED: Use 'cli-standard' instead
dev-minimal = ["cli-core"]

# DEPRECATED: Use 'cli-core' instead
dev-ultra-minimal = ["cli-core"]

# DEPRECATED: Use 'cli-standard' instead
dev-full = ["cli-standard"]

# DEPRECATED: Enterprise features removed for CLI release
production = ["cli-full"]
production-secure = ["cli-full"]
web = []
web-client = []
web-server = []
web-full = []
tui = []
# Legacy security feature flag kept for compatibility; security is now part of cli-standard.
crypto-minimal = []
crypto-full = []
prometheus = []
memory-optimization = []
chaos = []
regression-detection = []
sla-monitoring = []
mimalloc = []
graphql-api = []
yaml = []
reqwest = []
image-rendering = []
interactive-reports = ["image-rendering"]
performance-testing = []

[dependencies]
# Use workspace dependencies for consistency
serde = { workspace = true }
serde_json = { workspace = true }
serde_yaml = { workspace = true }
tokio = { workspace = true }
futures = { workspace = true }
anyhow = { workspace = true }
thiserror = { workspace = true }
chrono = { workspace = true }
reqwest = { workspace = true, optional = true }
dirs = { workspace = true }
levenshtein = { workspace = true }
# REMOVED: rustls, ring - crypto/security features removed for CLI release
# rustls = { workspace = true, optional = true }
# ring = { version = "0.17.14", optional = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }

# Project-specific dependencies
rusqlite = { version = "0.37.0", features = ["bundled"], optional = true }
tokio-stream = { version = "0.1.17", features = ["fs"], optional = true }
async-stream = { version = "0.3", optional = true }
bincode = { version = "1.3.3", optional = true }
clap = { version = "4.5.4", features = ["derive", "env"] }
# Unified logging with tracing (replaces log + env_logger)
# log = "0.4.21"        # Replaced by tracing
# env_logger = "0.11.3"  # Replaced by tracing-subscriber
regex = "1.11.2"
rayon = { version = "1.8.0", optional = true }
# AST parsing dependencies (optional via features)
tree-sitter = { version = "0.25.8", optional = true }
tree-sitter-rust = { version = "0.24.0", optional = true }
tree-sitter-python = { version = "0.23.6", optional = true }
tree-sitter-javascript = { version = "0.23.1", optional = true }
tree-sitter-typescript = { version = "0.23.2", optional = true }
# HTML escaping for fallback diagram rendering
html-escape = "0.2"
# Additional tracing appender
tracing-appender = "0.2"
# Time handling for logging timestamps
time = { version = "0.3", features = ["formatting", "local-offset"] }
# REMOVED: prometheus - monitoring feature removed for CLI release
# prometheus = { version = "0.14.0", optional = true }
rand = "0.9.2"
# LRU cache for bounded memory management (UV-152)
lru = "0.16.0"
# Concurrent HashMap for thread-safe caching
dashmap = "6.1.0"
# Template engine for diagram generation
tera = "1.19.1"
# Base64 encoding/decoding for image handling
base64 = "0.22.0"
# File system event watching for cache invalidation
notify = { version = "7.0", optional = true }
# MD5 hashing for input validation
md5 = "0.8.0"
# UUID generation for component IDs
uuid = { version = "1.8.0", features = ["v4", "v5", "serde"] }
url = "2.5"
walkdir = "2.5.0"
ignore = "0.4.22"
toml = "0.9.5"
sha2 = "0.10.0"
async-trait = "0.1.88"
petgraph = "0.8.2"
color-eyre = "0.6"
ndarray = "0.16.1"
strum = { version = "0.27.2", features = ["derive"] }
strum_macros = "0.27.2"
pastey = "0.1"
# WASM plugin system dependencies (optional via features)
wasmtime = { version = "36.0.1", features = ["component-model", "async"], optional = true }
wasmtime-wasi = { version = "36.0.1", optional = true }
cap-std = { version = "3.0", optional = true }
# REMOVED: Apache Arrow dependencies - data science features removed for CLI release
# arrow = { version = "56.1.0", optional = true }
# arrow-ipc = { version = "56.1.0", optional = true }
# arrow-schema = { version = "56.1.0", optional = true }
# arrow-array = { version = "56.1.0", optional = true }
wit-bindgen = { version = "0.44.0", optional = true }
# REMOVED: axum - web server feature removed for CLI release
# axum = { version = "0.8.4", features = ["ws", "macros"] }
# REMOVED: tokio-tungstenite - WebSocket support removed for CLI release
# tokio-tungstenite = { version = "0.27.0", optional = true }
criterion = { version = "0.7.0", features = ["html_reports"] }
sysinfo = "0.37.0"
# REMOVED: Advanced memory optimization dependencies - enterprise features removed for CLI release
# mimalloc = { version = "0.1", optional = true }
# bumpalo = { version = "3.19", features = ["collections", "boxed"], optional = true }
# bumpalo-herd = { version = "0.1", optional = true }
# memmap2 = { version = "0.9", optional = true }
# rkyv = { version = "0.8.11", optional = true }
# CPU count for pool sharding
num_cpus = "1.16"
# Lazy static for global instances
lazy_static = "1.4"

# REMOVED: TUI dependencies - terminal UI feature removed for CLI release
# ratatui = { version = "0.29", optional = true }
# crossterm = { version = "0.29", optional = true }
# tui-input = { version = "0.14", optional = true }

# Input validation framework
validator = { version = "0.20.0", features = ["derive"] }

# Configuration Management (for TUI settings)
# REMOVED: confy - TUI configuration removed for CLI release
# confy = "1.0"

# State Persistence (for TUI state)
# REMOVED: persisted - TUI state persistence removed for CLI release
# persisted = "1.0"

# Pattern matching for file filtering
glob = "0.3.1"

# MessagePack serialization removed - using bincode for binary serialization
# rmp-serde = "1.1"  # Redundant with bincode

# Compression for diagram caching
flate2 = "1.0"

# REMOVED: Chaos engineering and performance monitoring - enterprise features removed for CLI release
# fail = { version = "0.5", optional = true }
# statrs = { version = "0.18.0", optional = true }
# ndarray-stats = { version = "0.6.0", optional = true }
# changepoint = { version = "0.14.2", optional = true }
# linfa = { version = "0.7", optional = true }
# influxdb2 = { version = "0.5", optional = true }
# metrics-exporter-prometheus = { version = "0.17.2", optional = true }
# tokio-metrics = { version = "0.4.4", optional = true }
# metrics = "0.24.2"
# metrics-tracing-context = "0.18.1"

# REMOVED: Security, RBAC, and authentication - enterprise features removed for CLI release
# tower = { version = "0.5.2", features = ["util"] }
# tower-http = { version = "0.6.6", features = ["cors", "limit", "timeout", "fs", "trace"] }
# tower_governor = { version = "0.8.0", optional = true }
# casbin = { version = "2.1", optional = true }
# oauth2 = { version = "5.0", optional = true }
# openidconnect = { version = "4.0.1", optional = true }
# jsonwebtoken = { version = "9.3", optional = true }
# argon2 = { version = "0.5", optional = true }
blake3 = "1.5"
# subtle = { version = "2.5", optional = true }
# vaultrs = { version = "0.7", optional = true }
# config = { version = "0.15.14", optional = true }
streaming-iterator = "0.1.9"

# Knowledge Library Dependencies (UV-335)
# Perfect Hash Functions for O(1) lookups
phf = { version = "0.13.1", features = ["macros"] }
tempfile = "3.8"

# Terminal detection for TUI validation
# REMOVED: is-terminal - TUI validation removed for CLI release
# is-terminal = "0.4"

# TUI binary removed - TUI now integrated into main CLI as 'uveddi tui'
# Main CLI binary
[[bin]]
name = "uveddi"
path = "src/main.rs"

# [[bin]]
# name = "tui_test"
# path = "src/bin/tui_test.rs"
# required-features = ["tui"]

[[bin]]
name = "dependency_analyzer"
path = "src/bin/dependency_analyzer.rs"

[[bin]]
name = "circular_dependency_demo"
path = "src/bin/circular_dependency_demo.rs"

[[bin]]
name = "simple_cycle_demo"
path = "src/bin/simple_cycle_demo.rs"

[dev-dependencies]
tempfile = "3.8"
tokio-test = "0.4"
mockall = "0.13.1"
mockito = "1.7.0"
assert_cmd = "2.0"
predicates = "3.1.3"
serde_json = "1.0"
rstest = "0.26.1"
criterion = { version = "0.7.0", features = ["html_reports"] }
iai-callgrind = "0.16.1"
serial_test = "3.0"
proptest = "1.4"
# Note: Coverage tools (tarpaulin, cargo-llvm-cov) are installed via CI, not as dependencies

[build-dependencies]
# PHF code generation for build-time index creation
phf_codegen = "0.13.1"
# JSON serialization for knowledge base processing
serde_json = "1.0"
# Compression for build-time knowledge library preparation
flate2 = "1.0"
# SHA-256 hashing for checksums
sha2 = "0.10"
# Chrono for build timestamp
chrono = { version = "=0.4.41", features = ["serde"] }

[[test]]
name = "analysis_engine"
path = "tests/analysis/engine.rs"

[[test]]
name = "application"
path = "tests/application.rs"

[[test]]
name = "config"
path = "tests/config.rs"

[[test]]
name = "cli_analyze"
path = "tests/cli_analyze.rs"

[[test]]
name = "migrate_command"
path = "tests/cli/migrate_command.rs"

[[test]]
name = "database_crud"
path = "tests/database_crud.rs"

[[test]]
name = "comprehensive_coverage"
path = "tests/coverage/comprehensive_coverage.rs"

[[test]]
name = "edge_case_testing"
path = "tests/coverage/edge_case_testing.rs"

[[test]]
name = "regression_prevention"
path = "tests/coverage/regression_prevention.rs"

# REMOVED: Security tests - security features removed for CLI release
# [[test]]
# name = "security_tests"
# path = "tests/security_tests.rs"

# Benchmarks removed for Alpha release

# Optimized build profiles for different development scenarios

# Ultra-fast development profile for rapid iteration
[profile.dev-ultra-fast]
inherits = "dev"
opt-level = 0          # No optimization for fastest compilation
debug = false          # Minimal debug info to reduce memory usage
debug-assertions = false # Skip debug assertions for speed
overflow-checks = false  # Skip overflow checks for speed
lto = false            # No link-time optimization
panic = 'unwind'       # Standard panic handling
incremental = true     # Enable incremental compilation
codegen-units = 256    # Maximum parallelism for compilation speed
rpath = false          # Disable rpath for faster linking

# Memory-optimized development profile (based on memory hierarchy research)
[profile.dev-fast]
inherits = "dev"
opt-level = 0          # No optimization for fastest compilation
debug = false          # Minimal debug info to reduce memory
debug-assertions = false
overflow-checks = false
lto = false           # No link-time optimization for speed
panic = 'unwind'      # Standard panic handling
incremental = true    # Enable incremental compilation
codegen-units = 16    # Maximum parallelism for multi-core builds

# Memory-efficient development with some optimization
[profile.dev-optimized]
inherits = "dev"
opt-level = 1          # Light optimization
debug = true           # Keep debug info for development
lto = false
incremental = true
codegen-units = 8      # Balance between parallelism and optimization

# Test profile optimized for CI/CD speed
[profile.test-fast]
inherits = "test"
opt-level = 0          # Fast compilation for tests
debug = false          # Minimal debug info for CI
incremental = true     # Incremental compilation for CI caching
codegen-units = 16     # Parallel compilation

# Release profile with performance optimizations
[profile.release]
opt-level = 3          # Maximum optimization
lto = "fat"            # Full link-time optimization for production
codegen-units = 1      # Single codegen unit for best optimization
panic = "abort"        # Abort on panic for smaller binaries
strip = true           # Strip debug symbols for smaller binaries

# Optimized release profile for size-constrained environments
[profile.release-small]
inherits = "release"
opt-level = "z"        # Optimize for size
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true

# Binaries
# Cache benchmarking utilities (disabled for CLI-only release)
# [[bin]]
# name = "cache-benchmark"
# path = "src/bin/cache_benchmark.rs"
# required-features = ["analysis-cache"]

# [[bin]]
# name = "cache-memory-profiler"
# path = "src/bin/cache_memory_profiler.rs"
# required-features = ["analysis-cache"]

# [[bin]]
# name = "detector-cache-validator"
# path = "src/bin/detector_cache_validator.rs"
# required-features = ["analysis-cache"]

# Examples
[[example]]
name = "doctor_demo"
path = "examples/doctor_demo.rs"

[[example]]
name = "simple_doctor_demo"
path = "examples/simple_doctor_demo.rs"

[[example]]
name = "progress_demo"
path = "examples/progress_demo.rs"

[[example]]
name = "phase1_complete_demo"
path = "examples/phase1_complete_demo.rs"
